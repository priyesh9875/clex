/*

What is allowed

    Reserved keywords: almost all that I know
    Delimiters: ; , () {} []
    Comments: All
    Identifiers: All
    Operators: +, -, /, *, %, <, =, >, >=, <=, ==, ++, --, +=, -=, *=, /=, &&, &, !, ~
    Headers and Macros
    Numbers and Strings
    

What is not allowed
    Everything for which you get error :)>

*/
#include<stdio.h>
#include"custom.h"

#define PI      3.14
// Comment
const int x = 50;
char b = 'b';
int main() {
    total = mult( a,b )
    total += div(a*10,b);
    total += add(a,10);
    total += sub(a,b);

    b = 1 + 4 * ( 2 + 1 ) asds"asd " a a-=3 ;
    ++b;
    b++;
    --b;
    b--;

    b = ++a + b--;
    return 0;
}